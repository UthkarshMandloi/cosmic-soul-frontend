import Features from '@/components/Features/Features';

export default function FeaturesPage() {
  // This is the main page component for the '/features' route.
  // It simply renders the 'Features' component, which contains the UI and logic
  // for displaying the application's features.
  return <Features />;
}